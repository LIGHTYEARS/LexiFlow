# LexiFlow 产品需求文档

> 文档类型：个人自用产品 PRD
>
> 产品形态：Chrome 扩展及其管理页面
> 文档重点：描述用户目标、可见能力、产品规则、边界和验收标准；不规定代码结构、数据表、接口或技术栈。

## 1. 文档状态与需求口径

本 PRD 使用三种状态，避免把调研结论、产品设想和已确认需求混为一谈。

- **已确认需求**：LexiFlow 当前应实现或保留的产品能力。除非另有优先级说明，均进入产品规划。
- **候选假设**：看起来可能有个人学习价值，但尚未由实际使用证明。候选假设不自动进入首版范围。
- **待确认事项**：需要用户选择、真实样本或试用结果才能决定的问题。在确认前不得以默认实现代替产品决策。

竞品采用某项功能，不构成 LexiFlow 必须采用它的理由。所有取舍只服务于个人使用价值。

## 2. 产品定义

### 2.1 产品目标

LexiFlow 的目标是把英文网页阅读中自然出现的学习机会，低摩擦地转化为可整理、可检索、可复习和可再次使用的个人英语知识。

用户在阅读技术文档、博客、论文或其他英文网页时，可以随手保存单词、短语、佳句和技术术语。LexiFlow 利用页面语境辅助解释和整理，尽量避免重复与错误合并，并通过间隔复习、错误记录和专项练习帮助用户长期掌握这些内容。

### 2.2 产品定位

LexiFlow 是个人英语阅读与表达学习工具，不是：

- 面向公众的课程平台；
- 多用户协作系统；
- 以社交、排行榜或内容分发为目的的产品；
- 替用户自动收藏整篇网页的资料库；
- 能保证语言判断完全正确的权威教师；
- 以商业差异化为目标的功能集合。

### 2.3 目标用户

产品只面向其所有者本人，典型使用情境包括：

- 经常阅读英文技术文档、博客、论文和产品文档；
- 希望积累真实语境中的单词、表达、佳句和技术术语；
- 希望从“看得懂”进一步达到“记得住、分得清、用得出”；
- 偏好本地保存、可配置模型和可导出数据；
- 愿意定期复习和整理，但不希望维护成本超过学习收益。

### 2.4 核心结果

产品成功时，用户应能：

1. 在不中断阅读的情况下理解并保存选中内容；
2. 得到与当前语境有关、可人工修订的解释；
3. 避免知识库被重复卡片和错误合并持续污染；
4. 找回内容的原始出处和上下文；
5. 通过间隔复习和专项练习巩固薄弱内容；
6. 始终掌握对数据、自动操作和模型行为的最终控制权。

## 3. 产品原则

### 3.1 阅读优先

采集和快速解释不得强迫用户离开当前页面，也不得把每次选中文字都默认解释为学习意图。用户完成有效选区后，产品默认只显示轻量触发按钮；只有用户点击按钮或使用快捷键后，才请求解释并展开浮层。浮层只展示完成当前判断所需的最少信息，详细内容按需展开。

### 3.2 学习价值优先于生成数量

产品不以卡片数量为目标。对低价值、重复或无法可靠判断的内容，应允许跳过、暂存或等待确认。

### 3.3 自动建议，用户保有最终决定权

系统可以解释、分类、查重和提出整理建议，但高影响操作必须由用户确认。系统不得静默删除正式卡片、覆盖手写内容或重置学习记录。

### 3.4 原始证据可追溯

解释和整理可以变化，但用户采集时的原文、页面来源和上下文应可回看。生成内容不得伪装成网页原文。

### 3.5 AI 反馈不是唯一答案

语言解释、自然度和语体判断可能存在差异。产品应区分较明确的语言错误、依赖语境的判断和风格偏好，不把单一生成结果包装为唯一正确表达。

### 3.6 本地、可恢复、可迁移

学习数据默认只保存在本地。用户可完整导出、恢复和迁移数据；关键批量操作应可撤销或从备份恢复。

## 4. 产品范围与优先级

### 4.1 P0：首个可长期使用的核心闭环

- 网页选区捕获、轻量触发按钮和解释浮层；
- 基于选区与上下文的快速解释；
- 单词、短语、佳句和技术术语卡片；
- 保存前查重和相似内容提示；
- 新建卡片、追加上下文和跳过重复；
- Inbox 缓冲区；
- 卡片浏览、搜索、编辑、暂停、归档和删除；
- 今日复习和 FSRS 调度；
- Again、Hard、Good、Easy 四档复习反馈；
- 复习与错误记录；
- 本地模型连接设置；
- 本地保存、完整备份和恢复。

### 4.2 P1：降低维护成本并强化学习

- Inbox 批量整理；
- 卡片关系和易混表达；
- 标签与高级筛选；
- 错误页和薄弱点汇总；
- 专项练习；
- 页面来源聚合；
- CSV 和 Markdown 导出；
- 用户可见的提示词与自动执行偏好；
- 基础学习统计。

### 4.3 候选能力：开放式英语表达反馈

对短回答、仿写和短篇书面输出进行诊断性反馈，是本轮调研后新增的候选方向。它尚未被确认为 P0 或 P1，不能因为竞品存在类似功能就直接加入交付范围。第 13 章描述候选体验、边界和待确认事项。

### 4.4 当前不在核心范围

- 多用户账户和社区；
- 多设备云同步；
- 真人教师或母语者在线批改；
- Chrome Web Store 公开发布；
- 移动端；
- 完整 PDF 阅读器和 OCR；
- 自动全文扫描与自动收藏；
- 正式 IELTS、TOEFL 等考试评分；
- 长篇学术论文、引用规范和事实核查；
- Anki APKG 直接生成。

“不在核心范围”不等于永久排除，只表示当前没有足够的个人需求证据。

## 5. 整体信息架构

### 5.1 页面划词触发按钮与解释浮层

服务于当前阅读动作：选区完成后先提供不打断阅读的轻量入口；用户明确触发后，再快速解释、查看重复状态、保存或忽略。

### 5.2 页面侧边栏

服务于当前页面：查看已采集内容、待确认项目和本页学习摘要。

### 5.3 插件 Popup

服务于快速入口：今日待复习、逾期卡片、Inbox 数量、本周新增和常用操作。

### 5.4 Dashboard

服务于完整管理，包含：

- 今日复习；
- Inbox；
- 全部卡片；
- 页面来源；
- 标签；
- 错误记录；
- 专项练习；
- 统计；
- 设置。

如果开放式英语表达反馈被确认，可增加独立入口；在确认前不固定其导航位置。

## 6. 核心用户旅程

### 6.1 阅读时采集

1. 用户在网页中选中英文文本；
2. 选区稳定后，附近出现轻量触发按钮；此时不自动请求模型，也不展开解释浮层；
3. 用户点击触发按钮或使用快捷键，解释浮层随即出现并显示选区和稳定加载状态；
4. 产品给出类型判断和简要中英文解释；
5. 产品提示是否已有相同或相似卡片；
6. 用户可直接保存、展开详情、编辑、改类型、查看已有卡片或忽略；
7. 保存后，产品明确告知结果：新建、进入 Inbox、追加到已有卡片、跳过重复、等待确认或失败；
8. 用户继续阅读，不必打开 Dashboard。

### 6.2 整理 Inbox

1. 用户进入 Inbox；
2. 产品为条目给出新建、追加、合并、关联、加标签或丢弃建议；
3. 每条建议显示目标、理由、确定程度和可能风险；
4. 用户逐条确认，或只批量确认低风险操作；
5. 执行后展示结果，并允许撤销最近一次批量整理。

### 6.3 今日复习

1. 用户进入今日复习；
2. 产品优先展示逾期、到期和学习中的卡片，再按设置加入新卡；
3. 用户自测或输入答案；
4. 产品显示参考答案、上下文和必要说明；
5. 用户选择 Again、Hard、Good 或 Easy；
6. 产品安排下一次复习，并记录本次表现；
7. 当用户遇到特定困难时，可补充错误类型或笔记。

### 6.4 专项练习

1. 用户从错误页、困难卡片、标签或手选卡片发起练习；
2. 产品说明本次练习重点和来源；
3. 用户完成一组针对性题目；
4. 产品给出答案与解释，并记录结果；
5. 用户可回到相关卡片查看原始语境；
6. 是否影响卡片的 FSRS 状态，遵循第 12.5 节规则。

## 7. 网页选区、触发按钮与快速解释

### 7.1 触发与关闭

- 选区非空、长度在产品可处理范围内且当前站点未禁用时，默认只显示轻量触发按钮，不自动请求模型或展开解释浮层；
- 触发按钮应在选区稳定后出现，避免用户拖动选区时反复闪现；
- 用户点击触发按钮或使用快捷键后，产品才提取所需上下文、请求解释并显示浮层；
- 用户可在设置中显式开启“选区后自动解释”。该偏好不是默认值；开启后可跳过轻量触发状态，直接显示解释浮层；
- 触发按钮和解释浮层均靠近选区，但不得遮挡选中内容或超出视口；
- 页面滚动、重新选区、点击页面空白和按 Esc 时，触发按钮与浮层的关闭行为应可预测；
- 用户可为特定站点停用划词功能；
- 页面本身不允许扩展运行时，应明确说明限制。

### 7.2 轻量触发状态

- 只显示可识别的 LexiFlow 触发按钮，不展示解释、重复判断或其他生成内容；
- 按钮应有可访问名称，并支持键盘聚焦和触发；
- 按钮出现本身不得发起模型请求；
- 用户未触发时可以继续复制、搜索、滚动或阅读，不产生保存或学习记录；
- 对明显无学习价值或无法处理的选区，可不显示按钮；产品不得仅凭一次未触发的选区推断用户偏好。

### 7.3 解释浮层首屏

- 选中文本；
- 类型判断；
- 中文简义；
- 英文简义；
- 相同或相似卡片状态；
- 保存、展开和忽略。

解释未完成时先展示稳定骨架和取消入口，不能让浮层突然跳位或阻塞页面操作。

### 7.4 展开内容

根据内容类型按需展示：

- 完整解释；
- 当前语境中的含义；
- 常见搭配或句型；
- 例句；
- 近义、反义或易混表达；
- 来源句子和页面信息；
- 相似卡片及建议操作；
- 编辑与保存入口。

不适用的内容不应为了填满版面而生成。例如技术术语不必强行给反义词，普通句子不必强行拆出多个语法点。

### 7.5 页面上下文

产品应在获得用户授权的当前页面中，提取足以解释选区的相邻句子、所在段落、相关标题、页面标题和 URL。

规则：

- 只取完成当前解释所需的上下文，避免无目的地发送整页内容；
- 用户可在保存前看到关键来源文本；
- 页面来源与模型生成例句必须明确区分；
- PDF、复杂编辑器、受保护页面或动态内容无法可靠提取时，应降级为仅处理选区并提示上下文不足；
- 页面中的指令性文字只作为被学习的内容，不得改变 LexiFlow 的产品规则或自动操作权限。

### 7.6 失败与降级

- 快速解释失败时，仍允许用户保存原文到 Inbox；
- 模型连接不可用、超时或返回内容不可用时，显示简短原因并提供重试；
- 保存失败不得显示成功状态；
- 重试不得产生重复卡片；
- 用户取消请求后，产品停止继续展示该次结果。

## 8. 卡片与个人知识库

### 8.1 卡片类型

#### 单词卡

用于单个单词及其当前语境中的义项。可包含词形、词性、发音、双语解释、用法、例句、近反义表达和多个来源上下文。

#### 短语 / 表达卡

用于固定搭配、惯用表达和可复用短语。可包含双语解释、使用场景、常见句型、语体、替代表达、易混表达、例句和来源。

#### 佳句卡

用于值得保留的完整句子、可仿写句式和写作素材。可包含译文、值得学习之处、可复用结构、仿写示例、改写示例和来源。

#### 技术术语卡

用于技术文档、论文或工程文章中的概念与术语。可包含所属领域、双语解释、相关概念、常见混淆、例子和来源。

### 8.2 所有卡片的共同信息

用户应能看到和维护：

- 卡片正文与类型；
- 解释和学习提示；
- 例句；
- 一个或多个来源上下文；
- 标签；
- 与其他卡片的关系；
- 个人笔记；
- 创建与最近更新时间；
- 当前学习状态、下次复习时间和复习历史；
- 历史错误；
- 内容是来自网页、用户编辑还是模型生成。

### 8.3 卡片关系

产品可表达以下用户可理解的关系：

- 词形或表达变体；
- 同义或近义；
- 反义；
- 易混淆；
- 同一词族；
- 某句型中的用法；
- 一般相关概念。

关系用于导航、查重和练习，不得因为“相关”就自动把两个概念合并。

### 8.4 卡片状态与操作

用户可以：

- 查看和编辑所有可见内容；
- 追加例句、上下文、笔记和关系；
- 重新请求解释，但先预览再替换现有内容；
- 暂停或恢复复习；
- 归档卡片；
- 删除卡片，并在删除前看到影响范围；
- 查看重要修改的前后差异。

用户手写内容的优先级高于模型建议。模型不得静默覆盖用户修改。

## 9. 查重、相似判断与 Inbox

### 9.1 保存前判断

每次保存前，产品应尽力判断当前内容与已有卡片的关系：

- 完全重复；
- 同一单词的词形变化；
- 同一表达的新上下文；
- 同一词的相同或不同义项；
- 表达变体；
- 近义但用法不同；
- 反义或易混；
- 仅主题相关；
- 无关。

### 9.2 用户可见的建议结果

产品可以建议：

- 跳过重复；
- 追加上下文或例句；
- 为已有卡片增加义项；
- 新建独立卡片；
- 建立关系；
- 放入 Inbox 等待确认；
- 建议丢弃低价值内容。

建议必须说明“为什么”，并显示将影响哪张卡片。确定程度不足或候选冲突时，默认进入 Inbox，不得强行合并。

### 9.3 Inbox 定位

Inbox 是采集与正式知识库之间的安全缓冲区，用于：

- 待确认内容；
- 解释不完整或确定程度较低的内容；
- 需要人工判断的重复、义项和合并问题；
- 批量采集后尚未整理的内容；
- 模型不可用时先保存的原始选区。

### 9.4 Inbox 操作

用户可以新建卡片、追加上下文、增加义项、合并、建立关系、加标签、暂停处理、丢弃或重新分析。

Inbox 条目应保留原始选区和来源，即使生成解释失败也不能丢失原始材料。

### 9.5 批量整理

- 每次只处理用户可审阅的一组条目，数量可调整；
- 每条建议显示操作、目标、理由、确定程度和风险；
- 用户可以逐条确认、修改建议、重新分析或丢弃；
- “批量确认”只应用于低风险且同类的操作；
- 批量执行前显示影响摘要，执行后显示逐项结果；
- 最近一次批量操作可撤销；无法完全撤销时必须在执行前说明。

## 10. 自动操作与安全边界

### 10.1 默认可自动完成的低风险操作

- 跳过完全重复且来源已存在的保存请求；
- 为明确相同的卡片追加新的来源上下文；
- 将新内容保存到 Inbox；
- 添加不会改变卡片核心含义的候选标签。

自动操作的开启范围由用户设置。即使自动完成，也要给出简短结果，并可从记录中追溯。

### 10.2 必须确认的操作

- 合并两张正式卡片；
- 把内容加入已有卡片的新义项；
- 修改正式卡片的核心解释或类型；
- 批量修改标签或状态；
- 丢弃 Inbox 项；
- 将一批 Inbox 条目转成正式卡片；
- 暂停、归档或删除卡片；
- 让专项练习结果影响 FSRS 状态。

用户可在设置中要求所有写操作都先确认。

### 10.3 禁止自动完成的操作

- 永久删除正式卡片；
- 大规模改写正式知识库；
- 覆盖用户手写笔记；
- 清空复习或错误历史；
- 重置学习进度；
- 清空所有数据；
- 将原始网页内容替换为模型生成内容。

### 10.4 不确定性表达

产品不得只用一个看似精确的分数替代理由。涉及自然度、语体、义项或合并判断时，应使用用户能理解的说明，例如：

- 明确相同；
- 很可能是同一用法；
- 可能相关，需要确认；
- 上下文不足，无法可靠判断。

## 11. 搜索、浏览与管理

### 11.1 全部卡片

用户可以按卡片类型、标签、来源网站、创建时间、更新时间、学习状态、到期情况、困难程度和错误次数筛选。

搜索范围包括卡片正文、解释、例句、标签、来源页面和个人笔记。

### 11.2 卡片详情

详情页集中展示卡片内容、来源、关系、复习状态、复习历史、错误历史和重要编辑记录，并提供第 8.4 节所述操作。

### 11.3 页面来源

用户可以按页面查看：

- 标题、URL、域名和采集时间；
- 从该页面采集的卡片；
- 各卡片类型数量；
- 尚在 Inbox 的相关条目。

### 11.4 标签

用户可以新建、重命名、删除和合并标签，查看标签下卡片，并批量添加或移除标签。删除标签不得删除卡片。

### 11.5 统计

基础统计服务于调整学习负担，而不是制造排行榜。可包括：

- 总卡片数和近期新增；
- 今日完成、到期和逾期复习；
- 连续学习天数；
- 不同类型和标签的分布；
- Again 和 Hard 的分布；
- 高频错误与困难卡片。

统计必须能追溯到实际记录；没有足够样本时不展示带有误导性的趋势结论。

## 12. 复习、错误记录与专项练习

### 12.1 FSRS 复习

产品使用 FSRS 安排卡片的长期复习。用户无需理解内部参数，但应能看到卡片是否为新卡、学习中、复习中或重新学习，以及下一次到期时间。

今日复习默认按以下优先级组织：

1. 逾期卡片；
2. 今日到期卡片；
3. 学习中或重新学习的卡片；
4. 在每日上限内加入的新卡。

用户可以调整每日新卡量、每日复习量、是否优先困难卡、提醒时间和默认复习方式。

### 12.2 四档反馈

- **Again / 完全忘了**：无法回忆或答案核心错误；
- **Hard / 困难**：勉强回忆、明显犹豫或仍有重要问题；
- **Good / 想起来了**：在合理努力下正确回忆；
- **Easy / 很简单**：快速、稳定地正确回忆。

产品应在首次使用时解释四档含义，避免用户把语言质量评价和记忆难度评价混为一谈。

### 12.3 复习方式

- **快速复习**：先回忆，再显示答案；
- **输入复习**：输入含义、表达或例句后对照；
- **完形填空**：在原句或新语境中补全目标表达；
- **仿写**：基于句型完成自己的句子；
- **区分练习**：在近义、易混表达或技术术语之间作选择并说明区别。

系统可按卡片类型推荐复习方式，用户始终可以切换。

### 12.4 错误记录

每次复习至少记录卡片、时间和四档反馈。根据练习形式，还可记录用户答案、参考答案、用时和个人笔记。

用户可以手动标记，系统也可以建议以下错误类型：

- 忘记含义；
- 认识但不会使用；
- 搭配错误；
- 与相似表达混淆；
- 语体不合适；
- 语境不合适。

系统建议不得覆盖用户判断。错误页应支持查看近期错误、高频错误、混淆关系和按类型或标签筛选，并提供专项练习入口。

### 12.5 专项练习

专项练习可来自：

- Again 或 Hard 记录；
- 用户标记的错误类型；
- 易混关系；
- 困难卡片；
- 高频失败标签；
- 用户手动选择的卡片。

支持的已确认练习形式：

- 中译英；
- 英译中；
- 完形填空；
- 选择题；
- 近义表达区分；
- 仿写；
- 技术术语解释；
- 错题回放。

每次练习应说明练习重点、题目来源、参考答案与解释，并能回到原始卡片和上下文。

默认情况下，专项练习结果记入练习历史，但不直接修改 FSRS 排程。是否允许某类练习结果影响排程，必须由用户显式开启，并在提交前说明影响。

### 12.6 开放答案的当前边界

在开放式英语表达反馈能力被确认前，仿写和解释题只需支持：

- 展示参考答案或参考表达；
- 允许用户自行比较和记录结果；
- 将题目和答案关联回原始卡片；
- 不宣称已经按母语者标准完成篇章评审。

这一区分防止把“有开放题”误写成“已经具备开放答案深度评审”。

## 13. 候选能力：开放式英语表达反馈

### 13.1 状态与目的

**需求状态：候选假设，尚未进入已确认交付范围。**

候选目标是：当用户写下短回答、仿写、邮件片段或短篇书面输出后，LexiFlow 提供可解释的诊断性反馈，帮助用户理解问题并完成修改。

该能力的理由只能是它对个人英语学习或实际写作有帮助，不能是“竞品有”“市场缺口”或“形成差异化”。

### 13.2 两种不同的使用目标

需要区分：

- **训练模式**：重点是让用户先理解问题并亲自修改；
- **修稿模式**：重点是尽快把当前文本改得可用。

两者可以共存，但一次使用必须明确当前优先目标。训练模式不能在用户开始思考前默认用完整改写替代原文；修稿模式也不应强迫用户完成一套学习流程。

是否同时支持两种模式仍属待确认事项。

### 13.3 候选输入范围

- 一句或数句自由回答；
- 仿写；
- 情境短回答；
- 摘要、观点表达；
- 日记、邮件片段和短文。

首轮验证应使用用户真实会写的内容决定字数范围，不预设竞品的长度限制。口语发音评估、长篇论文和正式考试评分不属于该候选能力的默认范围。

### 13.4 候选反馈目标

用户可选择本次最关心的目标，而不是笼统地要求“按母语者标准”：

- 找出较明确的语言错误；
- 使表达在指定语境下更自然；
- 检查正式、学术、技术或日常语体；
- 检查内容是否清楚并完成沟通目的；
- 只看少量高优先级问题；
- 尽量全面地检查达到合理确定程度的问题。

“反馈范围”“评价标准”和“反馈语气”是不同设置，不得把“严格”同时解释为问题更多、标准更高和措辞更尖锐。

### 13.5 候选训练流程

1. 用户选择任务、学习目标和必要语境，或直接提交自由文本；
2. 用户独立完成表达；
3. 产品先给整体理解和值得保留之处；
4. 产品按优先级把问题定位到原文；
5. 每个问题说明性质、原因、适用语境和一个可执行的修改方向；
6. 用户自行修改；
7. 产品对比修改前后，并按需展示参考表达；
8. 用户决定是否保存本次记录或其中某个问题。

这是待验证流程，不代表每一步都必须最终保留。修稿模式可直接展示修改建议，不强制经过第 3、6 步。

### 13.6 候选反馈内容

根据任务需要选择以下维度，不要求每次全部展示：

- **任务达成**：是否回应题目或完成沟通目的；上下文不足时明确说明；
- **读者理解**：表达是否清楚、连贯；
- **语言正确性**：语法、拼写和句法；
- **自然度与搭配**：在指定地区、语境和语体下是否常见；
- **语境与语体**：正式程度、礼貌和专业场景是否合适；
- **文本组织**：结构、衔接、重点和冗余；
- **亮点**：值得保留的准确或有表现力的表达。

每条问题应尽量区分：

- 较明确的错误；
- 可以理解但不常见或不适合当前语境；
- 风格偏好；
- 因上下文不足而不确定的判断。

### 13.7 候选参考版本

产品可以提供：

- **最低必要修订**：修正明确错误和妨碍理解之处，尽量保留原措辞与结构；
- **自然表达示例**：在不改变核心意思的前提下，给出符合指定语境的一种或多种候选表达，并标明较大改动。

两者都是参考，不是唯一正确答案。模型生成的“自然表达示例”不得描述为真实母语者已经审核的结论。

### 13.8 权威性与安全边界

- 不宣称反馈等同于母语教师或真人母语者批改；
- 不保证用户达到母语者水平；
- 不把自然度或风格偏好显示为绝对对错；
- 用户原意不清时先指出假设，不擅自补写事实或立场；
- 反馈与改写可能出错，用户可以忽略、修改或重新请求；
- 总分或等级若存在，只能作为参考，不能代替逐项理由；
- 不提供正式考试认证分数；
- 不因一次成功修改就宣称用户已经长期掌握。

### 13.9 与错误记录、FSRS 和专项练习的关系

这里必须区分两个概念：

- **不以差异化为理由强行连接**：不为了让功能看起来完整或区别竞品，就自动把所有反馈写入知识库；
- **仍可基于个人学习价值连接**：如果实际使用证明某类错误值得长期复习，可以选择保存并用于后续学习。

候选规则：

- 默认只保留在本次反馈记录中，不自动创建卡片；
- 用户可选择保存某个问题、表达或修改版本；
- 保存前说明它将进入错误记录、现有卡片、新卡片还是专项练习；
- 不因写作反馈自动修改现有卡片的 FSRS 状态；
- 是否生成后续练习、何时退出练习和是否影响 FSRS，均需另行确认。

### 13.10 待确认事项

在将该能力排入交付范围前，需要确认：

1. 主要用途是训练写作、完成真实文本，还是两者都要；
2. 最常提交的是仿写、短回答、邮件还是短文；
3. 是否需要独立入口，还是只出现在专项练习中；
4. 最常用的反馈目标和语体；
5. 更偏好少量重点还是全面检查；
6. 是否必须先自行修改；
7. 是否需要最低必要修订和自然表达示例两种版本；
8. 是否长期保存原稿、用户修改稿和参考稿；
9. 哪些问题值得进入错误记录、卡片或专项练习；
10. 是否需要总分、等级或长期趋势；
11. 是否需要修稿模式；
12. 是否接受纯 AI 反馈的边界，还是未来需要真人复核。

### 13.11 候选能力的验证标准

若决定试做，应通过真实个人样本判断，而不是只验证“能生成一段反馈”：

- 用户能明确知道本次反馈针对什么目标和语境；
- 问题能定位到原文，并区分错误、自然度建议和风格偏好；
- 建议说明原因，不只给替换文本；
- 修改版本不擅自改变用户原意；
- 用户能接受、忽略或自行改写建议；
- 训练模式下，用户能够在看完整参考稿前完成自己的修改；
- 反馈记录不会未经确认污染卡片、错误历史或 FSRS；
- 一组真实样本试用后，用户愿意继续使用，而不是只觉得结果“看起来完整”。

通过这些标准只表示候选体验值得保留，不证明反馈达到真人教师水平或带来长期学习效果。

## 14. 设置

### 14.1 模型连接

用户可以配置用于 LexiFlow 的 LiteLLM 地址、凭证和模型，并执行连接测试。连接失败时应说明是无法访问、认证失败、超时还是模型不可用，同时避免在错误详情中暴露完整凭证。

### 14.2 任务模型与提示词

用户可以为快速解释、完整分析、Inbox 整理和专项练习选择模型。若开放式表达反馈被确认，再增加对应任务设置。

用户可以查看、编辑和恢复各任务的提示词，并知道当前使用的是默认版本还是自定义版本。更新默认提示词时不得静默覆盖用户自定义内容。

### 14.3 自动执行偏好

用户可以选择：

- 选区后仅显示轻量触发按钮，还是自动打开解释浮层；默认仅显示轻量触发按钮；
- 自动跳过明确重复；
- 自动追加明确相同内容的新上下文；
- 新内容默认进入 Inbox 还是直接进入正式卡片库；
- 自动添加候选标签；
- 所有写操作均要求确认。

产品不要求用户设置看似精确但难以理解的内部阈值。高级控制若提供，必须同时说明它会改变哪些可见行为。

### 14.4 复习偏好

- 每日复习和新卡上限；
- 新卡从何时开始复习；
- 提醒时间；
- 是否优先逾期或困难卡；
- 默认复习方式；
- 是否启用专项练习。

FSRS 的高级参数可以提供，但应有安全默认值、用途说明和恢复默认入口。

### 14.5 数据与站点控制

- 导出与导入；
- 手动备份与恢复；
- 清空 Inbox；
- 清空全部数据；
- 站点启用或禁用列表；
- 查看本地数据占用；
- 重建搜索能力。

清空数据必须二次确认，并提示先备份。

## 15. 数据、隐私与恢复

### 15.1 本地保存

- 卡片、来源、复习、错误、练习、设置和操作记录默认保存在本地；
- 默认只向用户配置的 LiteLLM 服务发送完成当前任务所需的内容；
- 如果该服务实际转发到第三方模型，产品应提醒数据将离开本机；
- 产品不得自行引入未配置的云端分析或同步服务；
- 凭证不得出现在普通页面内容、导出学习材料或错误截图中。

### 15.2 导出

必须支持完整备份格式，能够恢复：

- 卡片及来源；
- Inbox；
- 标签与关系；
- 复习和错误历史；
- 专项练习记录；
- 用户设置；
- 如果开放式表达反馈被确认并由用户选择长期保存，则包括相应记录。

另支持 CSV 和 Markdown 作为便于阅读或迁移的导出格式，但它们不必承担完整恢复能力。

### 15.3 导入与恢复

- 导入前先展示文件概况和将产生的变化；
- 导入不得在无提示时覆盖现有数据；
- 支持恢复完整备份；
- 导入或升级前自动创建可恢复备份；
- 恢复失败时保留操作前数据并给出可执行的处理建议。

### 15.4 批量操作恢复

合并、批量转正、批量标签和批量归档等操作应保留可理解的操作记录。用户可以撤销最近一次操作；若某种操作无法完全撤销，必须事先说明并提示备份。

## 16. 快捷操作与可用性

### 16.1 快捷键

默认提供以下能力，并允许用户在浏览器支持范围内自定义：

- 打开当前选区的浮层；
- 保存当前选区；
- 展开解释；
- 加入 Inbox；
- 开始今日复习；
- 关闭浮层。

具体按键若与系统或网站冲突，应允许修改，不把某组键位写成不可变产品规则。

### 16.2 状态反馈

所有耗时操作都应让用户知道当前处于等待、生成、完成、失败、超时或已取消状态。

轻量触发按钮只表达“可使用 LexiFlow 处理当前选区”，不得伪装成解释已经开始。用户触发后的页面浮层使用简短提示，例如“无法连接模型，可保存到 Inbox”；Dashboard 可以展示更详细的原因和解决步骤。

### 16.3 无障碍与阅读体验

- 主要操作可通过键盘完成；
- 焦点顺序和关闭行为可预测；
- 不只依赖颜色表达成功、风险和错误；
- 触发按钮和浮层在常见缩放比例和窄视口下仍可操作；
- 用户设置的字体大小和减少动画偏好应得到尊重。

## 17. 非功能目标

### 17.1 响应体验

- 有效选区稳定后，轻量触发按钮目标在 200 毫秒内可见；
- 用户点击按钮或使用快捷键后，解释浮层框架目标在 200 毫秒内可见；
- 本地模型正常可用时，从用户明确触发开始，快速解释目标在 1–3 秒内返回首个有用结果；
- 模型较慢时持续展示进度和取消入口，而不是让用户误以为插件失效；
- 保存、滚动和关闭浮层不得等待解释完成；
- 大量卡片和 Inbox 条目下，列表仍可持续浏览和筛选。

这些是产品体验目标。若真实本地模型条件无法稳定达到，应保留即时触发反馈、快速出现的浮层框架和异步结果，而不是牺牲阅读流畅度。

### 17.2 可靠性

- 同一保存请求重试不会重复创建内容；
- 模型返回不可用时不写入伪造的完整卡片；
- 批量操作部分失败时，逐项显示成功与失败结果；
- 浏览器重启后，已确认保存的数据和学习进度仍然存在；
- 数据升级失败时可恢复到升级前状态。

### 17.3 可维护的个人负担

- 用户能限制每日新卡和复习量；
- Inbox 堆积时提供分批整理，不通过强提醒制造压力；
- 标签和关系不是保存卡片的必填维护任务；
- 统计服务于发现负担和薄弱点，不以连续打卡惩罚用户。

## 18. 风险与产品应对

### 18.1 生成内容错误

风险：解释、例句、自然度或合并建议可能错误。

应对：保留来源、标注生成内容、允许编辑和重新分析；不确定内容进入 Inbox；高影响操作要求确认。

### 18.2 知识库污染

风险：重复卡片、错误合并和过量生成使知识库难以维护。

应对：保存前查重、低价值内容可跳过、默认谨慎合并、批量操作可撤销。

### 18.3 Inbox 堆积

风险：缓冲区变成新的收藏坟场。

应对：展示积压规模、支持小批量处理和安全批量确认；允许原始材料先保存，但不过度生成字段。

### 18.4 复习负担过重

风险：新增速度超过复习能力，导致逾期和弃用。

应对：每日上限、暂停和归档、优先到期内容、显示新增对未来负担的影响。

### 18.5 自动建议显得过度权威

风险：用户把模型建议当作唯一正确语言判断。

应对：区分错误、语境判断和风格偏好；提供理由和替代表达；不冒充真人母语者或认证评分。

### 18.6 本地数据丢失

风险：浏览器数据清理、导入错误或升级失败造成学习记录丢失。

应对：备份提醒、导入与升级前自动备份、恢复验证和批量操作记录。

## 19. 验收标准

### 19.1 划词采集

- 用户选中可处理的英文单词、短语或句子后，默认只出现轻量触发按钮，不自动请求模型或展开解释浮层；
- 用户可以继续复制、搜索、滚动或点击页面空白，而不会因为仅形成选区就创建记录；
- 用户点击触发按钮或使用快捷键后，浮层能够出现且不阻塞页面；
- 触发后的浮层能展示与选区相关的中英文解释或明确的失败状态；
- 用户能在详细解释未完成时把原文和来源保存到 Inbox；
- 保存结果明确显示为新建、追加、跳过、待确认或失败；
- 同一操作重试不会创建重复内容。

### 19.2 查重与安全

- 完全重复内容不会新建另一张正式卡片；
- 词形、义项、表达变体和仅主题相关的内容不会被统一当成“重复”；
- 每个合并或追加建议能看到目标卡片和理由；
- 两张正式卡片的合并必须确认；
- 系统不能自动删除正式卡片、覆盖手写笔记或重置学习记录。

### 19.3 卡片与来源

- 用户能查看、搜索、筛选和编辑全部卡片；
- 四类卡片都能保存适合其类型的信息，而不是强制使用同一内容模板；
- 每张采集卡片都能回到至少一个来源上下文；
- 网页原文、用户内容和模型生成内容可区分；
- 用户能暂停、恢复、归档和删除卡片。

### 19.4 Inbox

- 用户能看到待处理条目及原始来源；
- 解释生成失败不会丢失已保存原文；
- 整理建议包含操作、目标、理由和风险；
- 用户能逐条确认并只批量确认低风险同类操作；
- 批量执行有影响摘要、逐项结果和撤销能力。

### 19.5 复习与错误记录

- 今日列表能正确展示逾期、到期、学习中和新卡；
- 用户能完成复习并选择四档反馈；
- 完成后能看到新的到期安排；
- 复习历史在重启后仍可查看；
- Again 和 Hard 记录能在错误页找到；
- 用户可以修改或补充系统建议的错误类型。

### 19.6 专项练习

- 用户能从历史错误、困难卡片或手选卡片生成至少一种专项练习；
- 每题能看到来源、参考答案和解释；
- 完成记录能回到相关卡片；
- 默认情况下，专项练习不会静默改变 FSRS；
- 若用户启用影响 FSRS 的规则，提交前能看到影响说明。

### 19.7 设置、隐私与恢复

- 用户能配置并测试 LiteLLM 连接；
- 错误信息不暴露完整凭证；
- 默认不向未配置的服务发送学习内容；
- 用户能导出完整备份并恢复到新的本地安装；
- 导入、升级和清空操作前有适当的预览、备份或确认；
- 恢复后卡片、来源、复习和错误历史可继续使用。

### 19.8 开放式英语表达反馈

该能力当前不属于首版验收。只有在第 13.10 节的关键事项被确认并排入交付范围后，才使用第 13.11 节作为候选验收依据。不得用“能生成改写文本”替代对反馈定位、理由、边界和用户控制的验证。

## 20. 发布判断

### 20.1 P0 可用判断

P0 达成不以页面数量或生成字段数量衡量，而以核心闭环是否可持续使用衡量：

> 阅读时采集 → 安全进入知识库 → 找回来源 → 按期复习 → 记录困难 → 完整备份与恢复

若任一环节会静默丢数据、污染正式卡片或让用户无法恢复，P0 不应视为可长期使用。

### 20.2 P1 可用判断

P1 应明显降低整理和复习负担：用户可以批量处理 Inbox、理解错误模式、完成专项练习，并在卡片规模增长后继续找到和维护内容。

### 20.3 候选能力进入范围的条件

开放式英语表达反馈或其他候选能力，只有同时满足以下条件才进入正式优先级：

- 对应明确、反复出现的个人使用情境；
- 用户能说清它解决的问题；
- 与现有功能的关系明确，不靠概念上的“完整闭环”证明必要性；
- 风险和权威性边界可接受；
- 真实试用表明收益大于新增操作和维护成本。

## 21. 待确认事项汇总

除第 13.10 节的开放式表达反馈问题外，产品仍需通过实际使用确认：

- 新内容默认进入 Inbox，还是在低风险情况下直接进入正式卡片库；
- 哪些追加上下文操作可以自动完成；
- 用户最常使用的复习方式；
- 专项练习结果在什么条件下可以影响 FSRS；
- 哪些统计真正帮助调整学习，而不是增加噪音；
- 卡片关系和标签的实际维护价值；
- 快速解释在真实本地模型条件下可接受的等待时间；
- 备份提醒频率和保留数量。

这些问题应通过个人真实使用逐步决定，不应由技术实现便利性代替产品取舍。
